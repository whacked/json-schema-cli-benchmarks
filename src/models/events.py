# generated by datamodel-codegen:
#   filename:  events.schema.json
#   timestamp: 2026-01-26T14:38:52+00:00

from __future__ import annotations

from enum import Enum
from typing import Literal, Optional, Union

from pydantic import AwareDatetime, BaseModel, Field, RootModel, conint


class Outcome(Enum):
    VALID = 'VALID'
    INVALID = 'INVALID'
    UNSUPPORTED = 'UNSUPPORTED'
    ERROR = 'ERROR'


class Mode(Enum):
    file = 'file'
    stdin = 'stdin'


class Operation(Enum):
    schema = 'schema'
    instance = 'instance'


class Status(Enum):
    ok = 'ok'
    unsupported = 'unsupported'
    error = 'error'


class EventEnvelope(BaseModel):
    case_id: str = Field(..., description='Case identifier from manifest')
    draft: str = Field(..., description='JSON Schema draft identifier')
    event: str = Field(..., description='Event type discriminator')
    input_id: Optional[str] = Field(
        None,
        description='Relative path of instance file (for instance operations only)',
    )
    job_id: str = Field(
        ..., description='Stable job hash (sha256 of canonical job dimensions)'
    )
    mode: Mode = Field(..., description='Input mode for the operation')
    operation: Operation = Field(
        ..., description='Operation type: schema validation or instance validation'
    )
    status: Status = Field(..., description='Execution status')
    tool: str = Field(..., description='Tool adapter name')
    tool_version: str = Field(..., description='Observed tool version string')
    ts: AwareDatetime = Field(..., description='ISO-8601 timestamp')


class BenchmarkResult(EventEnvelope):
    event: Literal['benchmark_result'] = 'benchmark_result'
    hyperfine_json_path: Optional[str] = Field(
        None, description='Path to raw hyperfine JSON output'
    )
    max_s: float = Field(..., description='Maximum execution time in seconds')
    mean_s: float = Field(..., description='Mean execution time in seconds')
    min_s: float = Field(..., description='Minimum execution time in seconds')
    runs: conint(ge=1) = Field(..., description='Number of benchmark runs')
    stddev_s: float = Field(..., description='Standard deviation in seconds')


class CorrectnessResult(EventEnvelope):
    event: Literal['correctness_result'] = 'correctness_result'
    exit_code: int = Field(..., description='Raw exit code from adapter')
    expected: bool = Field(..., description='Expected validity from manifest')
    match: bool = Field(..., description='Did outcome match expected?')
    outcome: Outcome = Field(..., description='Interpreted validation outcome')
    stderr_path: Optional[str] = Field(None, description='Path to captured stderr')
    stdout_path: Optional[str] = Field(None, description='Path to captured stdout')


class BenchmarkEvent(RootModel[Union[CorrectnessResult, BenchmarkResult]]):
    root: Union[CorrectnessResult, BenchmarkResult] = Field(
        ...,
        description='Event record for benchmark runs (append-only events.jsonl)',
        title='Benchmark Event',
    )
