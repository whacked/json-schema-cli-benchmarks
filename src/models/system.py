# generated by datamodel-codegen:
#   filename:  system.schema.json
#   timestamp: 2026-01-29T17:11:29+00:00

from __future__ import annotations

from typing import Optional

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint


class SystemInformation(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    architecture: str = Field(..., description='CPU architecture (x86_64, arm64)')
    command: str = Field(
        ..., description='Full command line used to invoke this benchmark run'
    )
    cpu_cores: Optional[conint(ge=1)] = Field(
        None, description='Number of CPU cores (if available)'
    )
    cpu_model: Optional[str] = Field(None, description='CPU model name (if available)')
    git_dirty: Optional[bool] = Field(
        None, description='Whether the git working tree has uncommitted changes'
    )
    git_sha: Optional[str] = Field(
        None, description='Git commit SHA of the benchmark repo (if available)'
    )
    hostname: str = Field(..., description='Machine hostname')
    platform: str = Field(..., description='OS platform (darwin, linux, win32)')
    platform_version: str = Field(..., description='OS version string')
    python_version: str = Field(..., description='Python version string')
    ram_bytes: Optional[conint(ge=0)] = Field(
        None, description='Total RAM in bytes (if available)'
    )
    run_id: AwareDatetime = Field(
        ..., description='ISO-8601 timestamp marking the start of this benchmark run'
    )
