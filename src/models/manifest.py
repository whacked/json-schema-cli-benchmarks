# generated by datamodel-codegen:
#   filename:  manifest.schema.json
#   timestamp: 2026-02-17T04:05:58+00:00

from __future__ import annotations

from enum import Enum
from typing import Any, Dict, Optional

from pydantic import BaseModel, ConfigDict, Field


class Draft(Enum):
    draft_04 = 'draft-04'
    draft_05 = 'draft-05'
    draft_06 = 'draft-06'
    draft_07 = 'draft-07'
    draft_2019_09 = 'draft-2019-09'
    draft_2020_12 = 'draft-2020-12'


class CaseSpec(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    description: Optional[str] = Field(
        None, description='Human-readable description of the test case'
    )
    instance_valid: Optional[bool] = Field(
        ...,
        description='Expected result of instance validation, or null if not applicable (when schema_valid is false)',
    )
    instances: Optional[str] = Field(
        None,
        description="Glob pattern for instance files relative to case directory (e.g. 'instances/**/*.json')",
    )
    meta: Optional[Dict[str, Any]] = Field(
        None, description='Arbitrary metadata for reporting/filtering'
    )
    schema_valid: bool = Field(
        ..., description='Expected result of schema validation against metaschema'
    )


class ExperimentManifest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    cases: Dict[str, CaseSpec] = Field(
        ..., description='Mapping from case_id to case specification'
    )
    draft: Draft = Field(..., description='JSON Schema draft identifier')
